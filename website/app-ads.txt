# app-ads.txt - IAB Tech Lab spec v1.0. Lines starting with # are ignored.
#
# Declares who may sell ad inventory in IDCT's Android apps.
#
# THIS COPY IS NOT THE ONE CRAWLERS READ. A crawler takes the developer website
# from the app's Play listing, reduces it to the bare domain and fetches only
# <root>/app-ads.txt, so the file that governs serving is
# https://idct.tech/app-ads.txt, owned by the org's apex Pages repo. This repo is
# a project page: everything under website/ serves beneath /weight-tracker/, so
# the most this copy can answer is https://idct.tech/weight-tracker/app-ads.txt,
# which no crawler requests. It exists so the record is next to the app it
# covers, and it is harmless.
#
# Keep the record line below byte-identical to the apex copy. Two app-ads.txt
# files on one domain that disagree is a worse position than having one, and
# only the apex copy has any effect, so if they ever drift this one is wrong by
# definition.
#
# google.com = Google AdMob; pub-6904561240517963 = the AdMob publisher id
# (account-wide, so one record covers every app on this account);
# f08c47fec0942fa0 = Google's TAG certification id, the same for all publishers.
google.com, pub-6904561240517963, DIRECT, f08c47fec0942fa0
